{
    "name": "webhook-server",
    "version": "1.0.0",
    "description": "A webhook server for handling incoming events",
    "main": "webhook.js",
    "scripts": {
        "start": "node webhook.js",
        "dev": "nodemon webhook.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "webhook",
        "express",
        "server"
    ],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "^4.18.2",
        "body-parser": "^1.20.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}